<!DOCTYPE html>
<html>
<head>
  <title>Admin - CAFE WORKS</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <script src="https://kit.fontawesome.com/bd92a181e9.js" crossorigin="anonymous"></script>
  
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap" rel="stylesheet">
  
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>
<body class="d-flex flex-column vh-100">
  <header class ="shadow-sm">
    <nav class="navbar navbar-expand-lg navbar-light">
        <div class="container">
          <a class="navbar-brand" href="<%= admin_cafes_path %>"><%= image_tag('logo.jpg', size: "60x60") %></a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
            <% if admin_signed_in? %>
              <li class="nav-item px-2">
                <%= link_to 'Cafes', admin_cafes_path, class:"nav-link text-dark" %>
              </li>
              <li class="nav-item px-2">
                <%= link_to 'Users', admin_users_path, class:"nav-link text-dark" %>
              </li>
              <li class="nav-item px-2">
                <%= link_to 'Log out', destroy_admin_session_path, method: :delete, class:"nav-link text-dark" %>
              </li>
            <% else %>
              <li class="nav-item px-2">
                <%= link_to "Log in", new_admin_session_path, class:"nav-link text-dark" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
  </header>
  <main class="mb-auto" style="margin-top: 120px;">
    <p class="notice"><%= notice %></p>
    <%= yield %>
  </main>
  <footer class="custom-bg pt-3 pb-3">
      <div class="container">
        <div class="row no-gutters">
          <div class="col-md-4">
            <div style="white-space: nowrap; font-size: 10px;">Logo generated by <a href="https://www.designevo.com/" style="color: black; font-weight: bold;">DesignEvo free logo designer</a></div>
            <div style="white-space: nowrap; font-size: 10px;">Photos by <a href="https://www.pexels.com/" style="color: black; font-weight: bold;">pexels.com</a></div>
          </div>
        </div>
      </div>
    </footer>
</body>
</html>